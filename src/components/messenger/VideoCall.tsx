// Call feature removed — see CallContext for history.
// This stub keeps existing imports compiling without rendering anything.
const VideoCall = (_props: Record<string, unknown>) => null;

export default VideoCall;
