// Clean deployment entrypoint for Fashion Studio.
// Importing the shared Forge handler preserves the same authentication,
// unified credit ledger and response contract while avoiding the stale live slug.
import "../forge-ai-tools/index.ts";