-- Fix search_path security warning - replace without dropping
CREATE OR REPLACE FUNCTION update_shadow_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql 
SECURITY DEFINER
SET search_path = public;